# 高级 Web 兼容控制台

本文记录源码部署使用的传统 Web 入口。Venus 1.0.0 桌面客户端使用项目制工作台，普通用户无需单独启动此控制台。

启动：

```bash
.venv/bin/live-clipper web
```

默认只监听 `127.0.0.1:8765`。如果需要局域网访问，显式运行：

```bash
.venv/bin/live-clipper web --host 0.0.0.0
```

不要把 Web 控制台暴露到公网。控制台包含渲染、清理和删除本地文件的操作。
