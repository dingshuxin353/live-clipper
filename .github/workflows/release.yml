name: Release (disabled; local process only)

on:
  workflow_dispatch:

permissions: {}

jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - name: Explain release policy
        run: |
          echo "Automatic tag releases are disabled."
          echo "Use the approved local asynchronous release procedure."
