# Cheap Model: Reverse Review

You will receive a proposed final clip. Act as a critical reviewer and list reasons it may be unsuitable for publishing.

Return strict JSON with `problems`, `risk_level`, and `recommendation`.

