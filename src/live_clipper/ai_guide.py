from __future__ import annotations

AI_ASSISTANT_GUIDE = """# Venus AI 使用说明

把这份说明交给当前 Agent，协助准备资源、检查项目和审阅材料。桌面端从项目发起处理；不要用旧全局模型设置替代项目分配。

## 开始之前

先自动检查用户允许访问的环境：Python 版本、ffmpeg、ffprobe 与源码路径。不确定录像和成片目录时再询问用户，一次只问 1 到 2 个问题。不要默认开启局域网访问，不删除用户原始视频。

不要让用户把 API key、Token、Cookie 或任何密钥粘贴到聊天窗口。密钥由用户在资源编辑器中输入；未提交密钥只在表单内存中暂存，离开后需要重新输入。不要从真实用户配置中抄出密钥，不把请求正文或模型原始响应贴进诊断报告。

## 准备资源

打开资源页，先明确选择本机或云端语音识别，再选择内容分析与审阅资源。首次设置内使用同一份资源配置，完成后返回原步骤。资源列表为空时不要替用户选择供应商或模型。

本机识别由模型把音频转成带时间戳的文字稿。先说明下载体积与用途，取得用户的下载授权后从资源页准备模型；可选 ModelScope 或 Hugging Face，完整安装可复用。处理录像时不会隐式下载。

云端识别会发送音频。AI 分析与审阅会发送所需转录文本。火山方舟、通义、GLM、Kimi 提供地址预设，用户仍需填写账号可用的模型；通义需要正确地域与业务空间。自定义服务需兼容 OpenAI Chat Completions。

只有点击“验证用途”才发送内置验证内容，可能产生费用。一次返回 OK 不足以证明分析或审阅可用。结果未知时先查询原操作，不自动重复付费请求，也不要删除参数后重试。

## 项目与恢复

项目分别选择语音识别、内容分析、AI 审阅；审阅可以明确复用本项目分析资源。未就绪资源允许保存在草稿或未启用项目中，不能启动依赖它的处理。定时任务在具体项目中设置，外部 Agent 的周期任务也应遵守项目范围。

共享资源修改只影响引用项目的新记录。旧记录保留冻结修订，凭据修复要求原账号、端点、地域、业务空间和模型身份不变。连接验证通过后仍需重新检查问题，再由用户继续处理。更换身份只能显式新建重跑；保留旧记录、断点与成片。

Venus 不再内置 Codex CLI。模型直连和 Claude Code 审阅保留；通用 MCP 和当前 Agent 协作仍可使用。实际依赖旧接入的记录不会被自动切到其他付费服务。

## 审阅材料

`brief` 生成 `review_brief.json`、`review_notes.md` 与 `selected_clips.template.json`。当前 Agent 或人工阅读候选后，写入 `selected_clips.json`，经校验再渲染。新材料使用中性名称，旧命名材料仍可读取。

`automation check` 返回 `requires_review`、`review_tasks`、`review_task_file`。当 phase 是 `needs_review_selection`，读取 `review_task.md` 和候选包，核对时间范围、片段独立性、标题与重复内容；`failed_needs_review` 应先诊断失败，不能盲目重跑。

通过 MCP 可以查看记录、读取审阅包、提交片段选择并请求渲染。删除与清理先 preview，再提交确认；不删除原始视频，也不绕过路径限制。旧全局扫描、重试与自动审阅不能借当前配置恢复身份不明的记录。

源码 CLI 的 `scan`、`pipeline`、`refine` 需要 `--project-run` 与 `--service-dir` 指定实际项目记录。`doctor` 检查环境，`smoke` 用合成数据检查本地链路；合成烟测不代表真实模型、真实短片或播放已经验收。
"""
