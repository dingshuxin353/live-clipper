# StarWork 工作区规则

## 开始前先读

1. `_系统/上下文/当前项目.md`
2. `_系统/任务/当前工作.md`
3. `.starwork/rules/index.md`（如果存在）

## 工作区目录说明

| 目录 | 用途 | 写入规则 |
|---|---|---|
| `docs/` | 存放用户确认后的最终成果。 | 写入或覆盖前需要用户确认。 |
| `work/` | 存放 AI 生成的草稿、方案和中间版本。 | 可以写入草稿、过程材料和中间版本。 |

## 写入边界

- 原始资料默认不改写，除非用户明确要求。
- 草稿、方案和中间版本先进入声明的可写工作区。
- 用户确认后的成果进入确认成果目录。
- 不要创建上表之外的默认目录，除非用户确认。

## 需要确认

- 修改身份或稳定偏好。
- 将草稿晋升为正式事实源。
- 改变工作台结构或顶层业务目录。
